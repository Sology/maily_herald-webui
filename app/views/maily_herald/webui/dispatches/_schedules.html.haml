%h2.border
  = tw("logs.scheduled_deliveries")

= smart_listing_controls_for :schedules, :class => "context-menu" do
  %form{:action => ""}
    = filter_box "schedules_filter"

= smart_listing_render :schedules
